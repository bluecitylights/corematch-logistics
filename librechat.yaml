version: 1.3.13
cache: true

mcpSettings:
  # The MCP server is reachable only through the private Compose network.
  allowedAddresses:
    - mcp:8001

mcpServers:
  corematch:
    type: streamable-http
    url: http://mcp:8001/mcp
    timeout: 30000
    serverInstructions: true
